#include <bits/stdc++.h>
using namespace std;

int main()
{

    string s[100];
    cin >> s;

    int nb, ns, nc;
    cin >> nb >> ns >> nc;

    int pb, ps, pc;
    cin >> pb >> ps >> pc;

    int r;
    cin >> r;

    return 0;
}